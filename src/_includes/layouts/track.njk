---
layout: layouts/base.njk
---
{% if personaId and not persona %}
{% set persona = personas | findPersona(personaId) %}
{% endif %}
<div class="track-layout" data-persona="{{ persona.id }}">
  <nav class="chapter-nav">
    <div class="chapter-nav-label">{{ persona.verb }}</div>
    <a href="/" class="chapter-nav-back">← All Paths</a>
    {% for ch in persona.chapters %}
    <a href="/{{ persona.id }}/{{ ch.slug }}/"
       class="chapter-nav-item{% if ch.slug == currentChapter %} active{% endif %}">
      {{ loop.index }}. {{ ch.title }}
    </a>
    {% if ch.slug == currentChapter %}
    {% set _headings = content | chapterHeadings %}
    {% if _headings.length %}
    <ul class="chapter-subnav">
      {% for h in _headings %}
      <li><a class="chapter-subnav-item" href="#{{ h.id }}">{{ h.text }}</a></li>
      {% endfor %}
    </ul>
    {% endif %}
    {% endif %}
    {% endfor %}
    <div style="padding: 16px; margin-top: auto;">
      <a href="/read/{{ persona.id }}/" style="font-size:11px; color: var(--muted);">Read it all →</a>
    </div>
  </nav>
  <main class="track-content">
    {{ content | safe }}
  </main>
</div>
